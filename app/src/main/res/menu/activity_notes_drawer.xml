<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/addNewNote" android:icon="@android:drawable/ic_input_add"
            android:title="New Note" />
        <item android:id="@+id/editNote" android:icon="@android:drawable/ic_menu_edit"
            android:title="Edit a Note" />
        <item android:id="@+id/viewAllNote" android:icon="@android:drawable/ic_menu_agenda"
            android:title="Notes" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item android:id="@+id/nav_share" android:icon="@android:drawable/ic_menu_share"
                android:title="Share" />
        </menu>
    </item>

</menu>